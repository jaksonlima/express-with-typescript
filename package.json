{
  "name": "express-estudo",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "cross-env NODE_ENV=production ts-node src/rootServer.ts",
    "dev": "cross-env NODE_ENV=development ts-node-dev --inspect --transpile-only --ignore-watch node_modules src/rootServer.ts",
    "build": "rm -rf dist/ && tsc"
  },
  "dependencies": {
    "cross-env": "^7.0.3",
    "express": "^4.17.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.13",
    "ts-node-dev": "^1.1.8",
    "typescript": "^4.4.3"
  }
}